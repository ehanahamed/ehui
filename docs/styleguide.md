# Styleguide

HTML form elements should use the label as a wrapper:

```html
<label>
    <input type="" name"" />
</label>
```

Unclosed elements should use a slash at the end
```html
<element attribute="" />
```
